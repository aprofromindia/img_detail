import * as React from 'react';
import { View, Text, StyleSheet } from 'react-native';

interface Props {
  image: string;
}

export default function DetailImage({ image }: Props) {
  return (
    <View>
      <Text>Sample</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
